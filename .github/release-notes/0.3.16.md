- 新增文件夹工作区：可打开文件夹，在左侧按需展开文件树，并从树中直接打开 Markdown 文件。
- 文件菜单新增新建文件、新建文件夹、新建窗口、打开文件夹、最近打开的文件夹、保存和另存为。
- 新增图片插入：从文件树点击图片或在编辑器右键选择“插入图片”，都会以当前 Markdown 文件为基准写入相对路径，并在预览区显示本地图片。
- 文件树支持收起、Ctrl+B 快捷切换及左右拖拽调整宽度，宽度会保存在本机。
- 最近打开的文件夹改为右侧二级菜单显示，靠近窗口边缘时会自动改为向左展开。
- 移除打开文件后的成功弹窗，调整文件树标题栏高度，使其与编辑器标题栏分割线对齐。
